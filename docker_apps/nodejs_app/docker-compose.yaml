version: '3'
services:
  api:
    image: ajocard-sample-app
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"

  web-cli:
    image: ajocard-sample-app
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge